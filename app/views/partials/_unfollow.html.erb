<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id), 
																											 html: { method: :delete }, 
																											 remote: true) do |f| %>
	<%= button_tag(class: "d-block mx-auto btn btn-danger") do %>
		<%= icon('fas', 'heart') %>
	<% end %>
<% end %>