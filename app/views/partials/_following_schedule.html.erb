<% if @following_schedule_items.any? %>
	<h3 class="text-center">Stream Shedule</h3>
	<ul class="list-group">
		<% @following_schedule_items.each do |s| %>
			<%= render 'partials/following_stream', s: s %>
		<% end %>
	</ul>
<% end %>